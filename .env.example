# The static frontend needs no secrets or NEXT_PUBLIC_* credentials.
# Local API configuration: copy .dev.vars.example to worker/.dev.vars.
# Cloudflare CLI credentials: copy .env.cloudflare.example to .env.cloudflare.
# Both real files are ignored by Git.
